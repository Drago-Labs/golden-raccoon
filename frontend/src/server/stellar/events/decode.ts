export function decodeStellarEvent(event: any) {\n  const topic0 = event.topic?.[0];\n  const label = topic0?.value?.toString() ?? \"\";\n  let kind = \"unknown\";\n  if (label.toLowerCase().includes(\"registry\")) kind = \"registry\";\n  else if (label.toLowerCase().includes(\"policy\")) kind = \"policy\";\n  else if (label.toLowerCase().includes(\"audit\")) kind = \"audit\";\n  return { kind, label, data: event.value ?? null, raw: event };\n}\n