import { frozenClock } from "./clock"; import { evaluateSequence } from "./evaluator"; import { orderObservations } from "./observationReader"; import type { LabRequest, LabResult } from "./schema"; import { summarize } from "./summary";
export function replayAlertRule(input: LabRequest): LabResult { frozenClock(input.frozenAt); const ordered = orderObservations(input.observations); const timeline = evaluateSequence(input.draft, ordered.items); const saved = input.saved ? evaluateSequence(input.saved, ordered.items) : null; const partial = ordered.outOfOrder || timeline.some((item) => item.outcome === "missing"); return { state: input.observations.length === 0 ? "empty" : partial ? "partial" : "complete", frozenAt: input.frozenAt, timeline, summary: summarize(timeline, saved), warnings: ordered.outOfOrder ? ["Input observations were out of order and were evaluated chronologically."] : [] }; }
