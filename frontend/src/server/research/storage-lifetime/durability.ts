export function durabilityExplanation(value: "persistent" | "temporary" | "not_applicable" | "unknown") { return value === "persistent" ? "Persistent entry; TTL can still expire and require restoration." : value === "temporary" ? "Temporary entry; expiry does not imply restorable state." : value === "not_applicable" ? "Durability is not attached to this key type." : "Durability evidence unavailable."; }
