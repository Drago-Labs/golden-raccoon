import type { ClaimableItem } from "@/server/research/claimable-balances/schema";
import { EligibilityPanel } from "./EligibilityPanel";
import { PredicateTree } from "./PredicateTree";
export function BalanceList({ items }: { items: ClaimableItem[] }) { if (!items.length) return <div className="rounded-2xl border border-white/10 p-5 text-sm text-white/55">No claimable balances were found in the bounded result.</div>; return <section className="space-y-3" aria-label="Claimable balances">{items.map((item) => <article key={item.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><div className="flex flex-wrap justify-between gap-3"><div><div className="font-semibold">{item.amount} {item.asset}</div><div className="mt-1 break-all font-mono text-xs text-white/40">{item.id}</div></div><span className="text-xs">{item.observationTime}</span></div><div className="mt-4 grid gap-3 lg:grid-cols-2"><div role="tree" aria-label="Claim predicate"><PredicateTree node={item.predicate} /></div><EligibilityPanel item={item} /></div><div className="mt-3 text-xs text-white/40">Sponsor: {item.sponsor ?? "none"} · {item.provenance}</div></article>)}</section>; }
