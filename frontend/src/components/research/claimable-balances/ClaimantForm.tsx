import type { FormEvent } from "react";
type Network = "stellar-testnet" | "stellar-pubnet";
export function ClaimantForm(props: { network: Network; pageSize: number; maxPages: number; busy: boolean; mismatch: boolean; onNetwork: (value: Network) => void; onPageSize: (value: number) => void; onMaxPages: (value: number) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) {
  const field = "h-11 rounded-xl border border-white/15 bg-black/25 px-3 text-sm";
  return <form onSubmit={props.onSubmit} className="glass-panel space-y-4 rounded-2xl border border-white/10 p-5"><div><h2 className="text-lg font-semibold">Bounded discovery</h2><p className="mt-1 text-sm text-white/50">Results use the observed ledger close time, never the browser clock.</p></div><div className="grid gap-3 sm:grid-cols-3"><label className="grid gap-1 text-xs">Network<select className={field} value={props.network} onChange={(e) => props.onNetwork(e.target.value as Network)}><option value="stellar-testnet">Stellar Testnet</option><option value="stellar-pubnet">Stellar Pubnet</option></select></label><label className="grid gap-1 text-xs">Page size<input className={field} type="number" min="1" max="50" value={props.pageSize} onChange={(e) => props.onPageSize(Number(e.target.value))} /></label><label className="grid gap-1 text-xs">Maximum pages<input className={field} type="number" min="1" max="5" value={props.maxPages} onChange={(e) => props.onMaxPages(Number(e.target.value))} /></label></div>{props.mismatch ? <div role="alert" className="text-sm text-amber-100">Selected network must match the connected wallet.</div> : null}<button disabled={props.busy || props.mismatch} className="rounded-full bg-[#d9a441] px-5 py-2.5 text-sm font-semibold text-black disabled:opacity-55">{props.busy ? "Reading balances…" : "Explore claimable balances"}</button></form>;
}
