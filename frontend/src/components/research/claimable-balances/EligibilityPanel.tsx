import type { ClaimableItem } from "@/server/research/claimable-balances/schema";
export function EligibilityPanel({ item }: { item: ClaimableItem }) { return <div className="rounded-xl bg-black/20 p-3 text-sm"><div className="flex justify-between gap-2"><span>Eligibility</span><strong>{item.eligibility}</strong></div><div className="mt-1 text-xs text-white/50">Predicate: {String(item.predicateResult)} · Trustline: {item.trustlineState}</div>{item.reasons.length ? <ul className="mt-2 list-disc pl-5 text-xs text-amber-100/80">{item.reasons.map((reason) => <li key={reason}>{reason}</li>)}</ul> : null}</div>; }
