export type Draft = { threshold: number; hysteresis: number; cooldownMinutes: number; direction: "high_is_bad" | "low_is_bad"; enabled: boolean };
export function DraftRuleEditor({ value, onChange }: { value: Draft; onChange(value: Draft): void }) { return <fieldset className="grid gap-3 sm:grid-cols-2"><legend className="font-semibold">Draft rule</legend><label>Direction<select value={value.direction} onChange={(e) => onChange({ ...value, direction: e.target.value as Draft["direction"] })}><option value="high_is_bad">High is bad</option><option value="low_is_bad">Low is bad</option></select></label><label>Threshold<input aria-label="Threshold" type="number" value={value.threshold} onChange={(e) => onChange({ ...value, threshold: Number(e.target.value) })} /></label><label>Hysteresis<input aria-label="Hysteresis" type="number" value={value.hysteresis} onChange={(e) => onChange({ ...value, hysteresis: Number(e.target.value) })} /></label><label>Cooldown minutes<input aria-label="Cooldown minutes" type="number" value={value.cooldownMinutes} onChange={(e) => onChange({ ...value, cooldownMinutes: Number(e.target.value) })} /></label></fieldset>; }
