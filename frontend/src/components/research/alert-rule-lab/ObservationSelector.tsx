export function ObservationSelector({ value, onChange }: { value: string; onChange(value: string): void }) { return <label className="grid gap-1">Observation fixture JSON<textarea aria-describedby="observation-help" rows={9} value={value} onChange={(e) => onChange(e.target.value)} className="rounded-xl bg-black/25 p-3 font-mono text-xs" /><span id="observation-help" className="text-xs text-white/50">Maximum 100 wallet- and network-scoped observations.</span></label>; }
