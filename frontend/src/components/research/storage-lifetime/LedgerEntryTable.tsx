import type { StorageEntryDiagnostic } from "@/server/research/storage-lifetime/schema"; import { DiagnosticDetails } from "./DiagnosticDetails"; export function LedgerEntryTable({ entries }: { entries: StorageEntryDiagnostic[] }) { return <section aria-label="Ledger entry diagnostics" className="space-y-3">{entries.map((entry) => <article key={entry.keyXdr} className="rounded-2xl border border-white/10 p-4"><div className="flex flex-wrap justify-between gap-2"><strong>{entry.kind} · {entry.durability}</strong><span>{entry.state}</span></div><div className="mt-2 break-all font-mono text-xs text-white/40">{entry.keyXdr}</div><div className="mt-2 text-sm">Live until: {entry.liveUntilLedger ?? "unknown"} · Remaining: {entry.remainingLedgers ?? "unknown"} ledgers</div>{entry.estimatedSecondsRemaining !== null ? <div className="text-xs text-white/45">Estimated {entry.estimatedSecondsRemaining}s at an assumed 5s ledger cadence.</div> : null}<DiagnosticDetails entry={entry} /></article>)}</section>; }
