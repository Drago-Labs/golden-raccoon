import type { StorageEntryDiagnostic } from "@/server/research/storage-lifetime/schema"; export function LifetimeChart({ entries }: { entries: StorageEntryDiagnostic[] }) { const known = entries.filter((entry) => entry.remainingLedgers !== null); if (!known.length) return null; const max = Math.max(1, ...known.map((entry) => entry.remainingLedgers!)); return <section aria-label="Remaining lifetime chart" className="rounded-2xl border border-white/10 p-5"><h2 className="font-semibold">Remaining lifetime</h2><div className="mt-4 space-y-3">{known.map((entry) => <div key={entry.keyXdr}><div className="mb-1 flex justify-between text-xs"><span>{entry.kind}</span><span>{entry.remainingLedgers} ledgers</span></div><div className="h-2 rounded bg-white/10"><div className="h-2 rounded bg-[#d9a441]" style={{ width: `${Math.max(1, entry.remainingLedgers! / max * 100)}%` }} /></div></div>)}</div></section>; }
