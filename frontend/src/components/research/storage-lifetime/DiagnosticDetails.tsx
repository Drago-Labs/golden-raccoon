import type { StorageEntryDiagnostic } from "@/server/research/storage-lifetime/schema"; export function DiagnosticDetails({ entry }: { entry: StorageEntryDiagnostic }) { return <details className="mt-2"><summary className="cursor-pointer text-xs text-[#f2c86d]">Evidence and next steps</summary><ul className="mt-2 list-disc pl-5 text-xs text-white/55">{entry.evidence.map((line) => <li key={line}>{line}</li>)}<li>Review restoration or TTL extension manually in a trusted wallet tool.</li></ul></details>; }
