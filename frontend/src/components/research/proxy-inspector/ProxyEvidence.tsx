import type { ProxyResult } from "@/server/research/proxy-inspector";
export function ProxyEvidence({ result }: { result: ProxyResult }) { return <section><h2 className="font-semibold">Evidence at block {result.blockNumber ?? "unavailable"}</h2><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-xs"><thead><tr><th>Type</th><th>Address</th><th>Observation</th></tr></thead><tbody>{result.observations.map((item, index) => <tr key={index}><td>{item.kind}</td><td className="font-mono">{item.address}</td><td>{item.detail}</td></tr>)}</tbody></table></div></section>; }
