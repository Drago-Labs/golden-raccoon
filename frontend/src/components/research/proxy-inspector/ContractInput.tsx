export function ContractInput(props: { address: string; network: string; busy: boolean; onAddress(v: string): void; onNetwork(v: string): void }) {
  return <><label className="grid gap-1">Network<select value={props.network} onChange={(e) => props.onNetwork(e.target.value)} className="h-11 rounded-xl bg-black/25 px-3"><option value="goat">GOAT Network</option><option value="ethereum">Ethereum</option><option value="base">Base</option></select></label><label className="grid gap-1">Contract address<input required value={props.address} onChange={(e) => props.onAddress(e.target.value)} className="h-11 rounded-xl bg-black/25 px-3 font-mono" /></label><button disabled={props.busy} className="rounded-full bg-[#d9a441] px-5 py-2 font-semibold text-black">{props.busy ? "Inspecting…" : "Inspect proxy"}</button></>;
}
