import { beforeEach, describe, expect, it, vi } from "vitest"; import { wallet } from "./fixtures";
const mocks = vi.hoisted(() => ({ explore: vi.fn(), session: vi.fn(), authz: vi.fn(), rate: vi.fn() })); vi.mock("@/server/research/claimable-balances", async () => ({ ...(await vi.importActual<typeof import("@/server/research/claimable-balances")>("@/server/research/claimable-balances")), exploreClaimableBalances: mocks.explore })); vi.mock("@/server/security/walletSession", () => ({ resolveWalletSession: mocks.session })); vi.mock("@/server/security/authz", () => ({ evaluateCapability: mocks.authz })); vi.mock("@/server/security/rateLimit", () => ({ checkRateLimit: mocks.rate })); import { POST } from "@/app/api/insights/claimable-balances/route";
describe("claimable route", () => { beforeEach(() => { vi.clearAllMocks(); mocks.rate.mockReturnValue(null); mocks.session.mockReturnValue({ wallet }); mocks.authz.mockReturnValue({ allowed: true }); mocks.explore.mockResolvedValue({ walletAddress: wallet, network: "stellar-testnet", state: "complete" }); }); it("uses session wallet", async () => { const response = await POST(new Request("http://x/api", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ walletAddress: wallet, network: "stellar-testnet", walletNetwork: "stellar-testnet" }) })); expect(response.status).toBe(200); expect(mocks.explore).toHaveBeenCalledWith(expect.objectContaining({ walletAddress: wallet })); }); it("stops before provider on session rejection", async () => { mocks.session.mockReturnValue({ response: new Response("forbidden", { status: 403 }) }); const response = await POST(new Request("http://x/api", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ walletAddress: wallet, network: "stellar-testnet", walletNetwork: "stellar-testnet" }) })); expect(response.status).toBe(403); expect(mocks.explore).not.toHaveBeenCalled(); }); });
