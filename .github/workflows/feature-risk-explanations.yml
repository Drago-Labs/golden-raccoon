name: Feature · risk explanations

on:
  pull_request:
    paths:
      - "frontend/src/server/research/risk-explanations/**"
      - "frontend/src/components/research/risk-explanations/**"
      - "frontend/src/app/insights/risk-explanations/**"
      - "frontend/src/app/api/insights/risk-explanations/**"
      - "frontend/tests/features/risk-explanations/**"
      - ".github/workflows/feature-risk-explanations.yml"
  push:
    branches: [main]

concurrency:
  group: feature-risk-explanations-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  focused-suite:
    name: risk-explanations focused suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Run focused suite
        working-directory: frontend
        run: npx vitest run tests/features/risk-explanations
