name: Feature · proxy inspector

on:
  pull_request:
    paths:
      - "frontend/src/server/research/proxy-inspector/**"
      - "frontend/src/components/research/proxy-inspector/**"
      - "frontend/src/app/insights/proxy-inspector/**"
      - "frontend/src/app/api/insights/proxy-inspector/**"
      - "frontend/tests/features/proxy-inspector/**"
      - ".github/workflows/feature-proxy-inspector.yml"
  push:
    branches: [main]

concurrency:
  group: feature-proxy-inspector-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  focused-suite:
    name: proxy-inspector focused suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Run focused suite
        working-directory: frontend
        run: npx vitest run tests/features/proxy-inspector
