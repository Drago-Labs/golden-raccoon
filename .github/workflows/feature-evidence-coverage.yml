name: Feature · evidence coverage

on:
  pull_request:
    paths:
      - "frontend/src/server/research/evidence-coverage/**"
      - "frontend/src/components/research/evidence-coverage/**"
      - "frontend/src/app/insights/evidence-coverage/**"
      - "frontend/src/app/api/insights/evidence-coverage/**"
      - "frontend/tests/features/evidence-coverage/**"
      - ".github/workflows/feature-evidence-coverage.yml"
  push:
    branches: [main]

concurrency:
  group: feature-evidence-coverage-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  focused-suite:
    name: evidence-coverage focused suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Run focused suite
        working-directory: frontend
        run: npx vitest run tests/features/evidence-coverage
