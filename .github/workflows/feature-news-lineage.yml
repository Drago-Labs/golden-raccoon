name: Feature · news lineage

on:
  pull_request:
    paths:
      - "frontend/src/server/research/news-lineage/**"
      - "frontend/src/components/research/news-lineage/**"
      - "frontend/src/app/insights/news-lineage/**"
      - "frontend/src/app/api/insights/news-lineage/**"
      - "frontend/tests/features/news-lineage/**"
      - ".github/workflows/feature-news-lineage.yml"
  push:
    branches: [main]

concurrency:
  group: feature-news-lineage-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  focused-suite:
    name: news-lineage focused suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Run focused suite
        working-directory: frontend
        run: npx vitest run tests/features/news-lineage
