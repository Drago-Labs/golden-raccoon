name: Feature · social coordination

on:
  pull_request:
    paths:
      - "frontend/src/server/research/social-coordination/**"
      - "frontend/src/components/research/social-coordination/**"
      - "frontend/src/app/insights/social-coordination/**"
      - "frontend/src/app/api/insights/social-coordination/**"
      - "frontend/tests/features/social-coordination/**"
      - ".github/workflows/feature-social-coordination.yml"
  push:
    branches: [main]

concurrency:
  group: feature-social-coordination-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  focused-suite:
    name: social-coordination focused suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Run focused suite
        working-directory: frontend
        run: npx vitest run tests/features/social-coordination
